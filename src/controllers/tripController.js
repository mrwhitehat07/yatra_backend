const User = require("../models/userModel");
const Trip = require("../models/tripModel");
const Location = require("../models/locationModel");
const { generateInvitationLink } = require("../controllers/globalController");

const createTrips = async () => {
    
}

const updateTrips = async () => {
   
}

const deleteTrips = async () => {
}

module.exports = {
    createTrips,
    updateTrips,
    deleteTrips,
}